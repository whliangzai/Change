# A股短线量化验证系统—数据库设计

| 项目 | 内容 |
|---|---|
| 文档名称 | 05-数据库设计 |
| 版本 | v1.1.0 |
| 状态 | 基线已决策（待实施） |
| 最后更新时间 | 2026-09-03 |
| 关联文档 | [需求规格说明书](./02-需求规格说明书.md)、[架构设计](./04-系统架构设计.md)、[API](./06-API接口设计.md)、[测试计划](./08-测试计划与测试用例.md) |

> 数据库基线已决策为 PostgreSQL 16；Docker Compose 负责首期运行编排。SQLite 仅用于单元测试夹具，字段类型按 PostgreSQL 表示。

交易规则、交易单位、T+1、涨跌停和费用字段均应绑定规则/成本版本；实施前复核官方来源与券商账单：[上交所交易规则（2026 年修订）](https://www.sse.com.cn/lawandrules/sselawsrules2025/stocks/exchange/c/c_20260424_10816482.shtml)、[深交所交易规则（2026 年修订）](https://docs.static.szse.cn/www/lawrules/rule/trade/current/W020260424690713155663.pdf)、[上交所收费及代收税费说明](https://one.sse.com.cn/onething/gptz/)、[深交所收费及代收税费标准](https://investor.szse.cn/marketServices/deal/payFees/index.html)。

## 1. 设计原则

1. 原始数据、配置版本、运行输入和结果分层保存；原始数据不被计算过程覆盖。
2. 所有日期均使用交易日语义，时间字段使用带时区时间戳；金额使用 `numeric(20,6)`，价格使用 `numeric(20,6)`，避免浮点结算。
3. 运行、订单、成交、账本和审计采用追加优先；更正通过新记录关联原记录，不物理删除。
4. 历史股票池必须由日期状态重建，不能仅由当前证券表筛选。
5. 关键外键和唯一约束由数据库保证，跨表业务约束由领域服务和测试保证。

## 2. 逻辑分层

| Schema/层 | 内容 |
|---|---|
| `ref` | 证券、交易日历、行业和规则字典 |
| `market` | 原始/标准化日线、复权因子、停复牌和历史状态 |
| `strategy` | 策略版本、参数快照、特征和信号 |
| `portfolio` | 账户、持仓、现金、计划订单、实际成交、费用 |
| `backtest` | 回测运行、阶段、每日账本、绩效指标和报告 |
| `ops` | 数据批次、任务、审计、导出和系统事件 |

## 3. 核心表与字段

### 3.1 身份、配置与数据

| 表 | 关键字段（类型） | 主键/外键/索引 | 说明 |
|---|---|---|---|
| `user_account` | `id uuid`、`username varchar(64)`、`password_hash text`、`status varchar(16)`、`created_at timestamptz` | PK `id`；UK `username` | 不保存券商密码 |
| `role` | `id uuid`、`code varchar(32)`、`name varchar(64)` | PK；UK `code` | `USER/REVIEWER/ADMIN` |
| `user_role` | `user_id uuid`、`role_id uuid` | PK(`user_id`,`role_id`)；FK | RBAC 关联 |
| `data_source` | `id uuid`、`name varchar(128)`、`kind varchar(32)`、`secret_ref varchar(256)`、`enabled boolean` | PK；UK `name` | 只保存密钥引用 |
| `data_batch` | `id uuid`、`source_id uuid`、`dataset_type varchar(32)`、`as_of_date date`、`available_at timestamptz`、`information_cutoff_at timestamptz`、`version varchar(64)`、`status varchar(16)`、`quality_summary jsonb`、`content_hash varchar(128)` | PK；FK source；UK(source_id,dataset_type,as_of_date,version) | 数据质量门禁；`available_at` 与 `information_cutoff_at` 均必填，用于证明数据何时可用及其信息截止时点 |
| `trade_calendar` | `exchange varchar(8)`、`trade_date date`、`is_open boolean`、`prev_trade_date date`、`next_trade_date date` | PK(exchange,trade_date)；IX next | 交易日计算 |
| `security` | `id uuid`、`symbol varchar(16)`、`exchange varchar(8)`、`security_type varchar(16)`、`list_date date`、`delist_date date` | PK；UK(exchange,symbol) | 证券主数据，保留退市记录 |
| `security_status_history` | `security_id uuid`、`effective_date date`、`is_st boolean`、`is_suspended boolean`、`is_delist_period boolean`、`board varchar(16)`、`source_batch_id uuid` | PK(security_id,effective_date)；FK | 按日历史状态 |
| `industry_membership_history` | `security_id uuid`、`industry_code varchar(32)`、`effective_from date`、`effective_to date`、`source_batch_id uuid` | PK(security_id,industry_code,effective_from)；IX date | 历史行业，不能用当前行业回填 |

### 3.2 行情与策略

| 表 | 关键字段（类型） | 主键/索引 | 说明 |
|---|---|---|---|
| `daily_bar` | `security_id uuid`、`trade_date date`、`raw_open/raw_high/raw_low/raw_close numeric(20,6)`、`adjusted_open/adjusted_high/adjusted_low/adjusted_close numeric(20,6)`、`volume numeric(24,6)`、`amount numeric(24,6)`、`adjust_factor numeric(20,10)`、`data_batch_id uuid` | PK(security_id,trade_date)；IX(trade_date,security_id) | 同时保留原始 OHLCV 与后复权 OHLC；信号/收益使用后复权，审计和成交解释保留原始口径；不保存未来修订覆盖 |
| `adjustment_factor` | `security_id uuid`、`effective_date date`、`factor numeric(20,10)`、`factor_type varchar(16)`、`data_batch_id uuid` | PK(security_id,effective_date,factor_type) | 复权因子独立可审计 |
| `rule_config_version` | `id uuid`、`version varchar(64)`、`scope varchar(32)`、`config jsonb`、`source_urls jsonb`、`effective_from date`、`status varchar(16)` | PK；UK(scope,version) | 涨跌停、交易单位、交易时间、T+1 等可配置；按生效日选择 |
| `cost_config_version` | `id uuid`、`version varchar(64)`、`commission_rate numeric(12,8)`、`commission_min numeric(20,6)`、`stamp_tax_sell_rate numeric(12,8)`、`transfer_rate numeric(12,8)`、`regulatory_fee_rate numeric(12,8)`、`handling_fee_rate numeric(12,8)`、`commission_includes_regulatory boolean`、`commission_includes_handling boolean`、`slippage_buy/sell numeric(12,8)`、`execution_price_mode varchar(32)`、`partial_fill_mode varchar(16)`、`config jsonb` | PK；UK version | v1.1.0 默认佣金/滑点/费用模型；防止规费重复计算 |
| `strategy_version` | `id uuid`、`code varchar(64)`、`version varchar(32)`、`status varchar(16)`、`parameters jsonb`、`change_reason text`、`created_by uuid`、`published_at timestamptz` | PK；UK(code,version)；IX status | 发布后不可修改 |
| `signal_snapshot` | `id uuid`、`strategy_version_id uuid`、`data_batch_id uuid`、`as_of_date date`、`information_cutoff_at timestamptz`、`security_id uuid`、`features jsonb`、`eligibility jsonb`、`score numeric(20,10)`、`exclusion_reason varchar(128)` | PK；UK(strategy,data,as_of_date,security)；IX(as_of_date,score) | 只保存 <=T 且在 cutoff 前可得的特征和理由 |

### 3.3 回测、账本、订单与报告

| 表 | 关键字段（类型） | 主键/外键/索引 | 说明 |
|---|---|---|---|
| `backtest_run` | `id uuid`、`run_no varchar(64)`、`data_batch_id uuid`、`strategy_version_id uuid`、`cost_config_id uuid`、`rule_config_id uuid`、`start_date/end_date date`、`train_end/valid_end date`、`oos_start date`、`benchmark_symbol varchar(16)`、`secondary_benchmark_symbol varchar(16)`、`universe_benchmark_enabled boolean`、`execution_price_mode varchar(32)`、`initial_equity numeric(20,6)`、`config_snapshot jsonb`、`status varchar(16)`、`result_usable boolean`、`created_by uuid` | PK；UK run_no；IX status/created_at | 不可变运行配置；主基准为 `000300.SH`，次基准为 `000001.SH`，同时记录股票池等权基准开关 |
| `run_stage` | `id uuid`、`run_id uuid`、`stage_code varchar(32)`、`status varchar(16)`、`started_at/ended_at timestamptz`、`error_summary text` | PK；UK(run_id,stage_code) | 断点和进度 |
| `portfolio_snapshot` | `id uuid`、`run_id uuid`、`account_id uuid`、`trade_date date`、`cash numeric(20,6)`、`market_value numeric(20,6)`、`equity numeric(20,6)`、`high_watermark numeric(20,6)`、`drawdown numeric(12,8)`、`risk_state varchar(24)` | PK；UK(run/account/date)；IX date | 每日权益快照 |
| `position_snapshot` | `id uuid`、`portfolio_snapshot_id uuid`、`security_id uuid`、`quantity integer`、`available_quantity integer`、`avg_cost numeric(20,6)`、`market_price numeric(20,6)`、`industry_code varchar(32)` | PK；UK(snapshot,security) | 反映 T+1 可卖数量 |
| `order_plan` | `id uuid`、`plan_no varchar(64)`、`run_id uuid`、`as_of_date date`、`execution_date date`、`security_id uuid`、`side varchar(8)`、`planned_quantity integer`、`reference_low/high numeric(20,6)`、`estimated_cost numeric(20,6)`、`trigger_reasons jsonb`、`risk_snapshot jsonb`、`status varchar(24)`、`idempotency_key varchar(128)` | PK；UK plan_no/idempotency_key；IX execution_date/status | 计划不等于成交 |
| `execution_record` | `id uuid`、`execution_no varchar(64)`、`plan_id uuid`、`execution_type varchar(16)`、`executed_at timestamptz`、`quantity integer`、`price numeric(20,6)`、`commission numeric(20,6)`、`stamp_tax numeric(20,6)`、`transfer_fee numeric(20,6)`、`other_fee numeric(20,6)`、`unfilled_quantity integer`、`unfilled_reason varchar(128)`、`source varchar(16)` | PK；UK execution_no；IX(plan_id,executed_at) | 模拟或人工录入成交 |
| `ledger_entry` | `id uuid`、`account_id uuid`、`run_id uuid`、`trade_date date`、`entry_type varchar(24)`、`security_id uuid`、`quantity_delta integer`、`cash_delta numeric(20,6)`、`fee_delta numeric(20,6)`、`source_id uuid`、`created_at timestamptz` | PK；IX(account,date) | 现金/持仓的可对账流水 |
| `performance_metric` | `id uuid`、`run_id uuid`、`segment varchar(16)`、`metric_code varchar(32)`、`metric_value numeric(24,10)`、`calculation_note text` | PK；UK(run,segment,metric) | 训练/验证/OOS/全区间指标 |
| `report_artifact` | `id uuid`、`run_id uuid`、`report_type varchar(24)`、`file_path text`、`content_hash varchar(128)`、`created_at timestamptz` | PK；IX run/type | 报告只读，不回写运行 |

### 3.4 日报、任务和审计

| 表 | 关键字段（类型） | 主键/索引 | 说明 |
|---|---|---|---|
| `daily_report` | `id uuid`、`report_date date`、`run_id uuid`、`status varchar(16)`、`market_switch boolean`、`content jsonb`、`result_usable boolean`、`content_hash varchar(128)` | PK；UK(report_date,run_id) | 日报版本化 |
| `job_run` | `id uuid`、`job_code varchar(32)`、`business_date date`、`idempotency_key varchar(128)`、`status varchar(16)`、`attempt integer`、`error_summary text`、`started_at/ended_at timestamptz` | PK；UK(job_code,idempotency_key) | 调度和重试 |
| `audit_event` | `id uuid`、`occurred_at timestamptz`、`actor_user_id uuid`、`actor_role varchar(32)`、`action varchar(64)`、`object_type varchar(32)`、`object_id varchar(64)`、`before_digest text`、`after_digest text`、`request_id varchar(64)`、`result varchar(16)` | PK；IX(object/time)、IX(actor/time) | 追加、不可由用户删除 |
| `system_alert` | `id uuid`、`alert_code varchar(32)`、`severity varchar(16)`、`source_ref varchar(128)`、`message text`、`status varchar(16)`、`created_at/acked_at timestamptz` | PK；IX(status,severity) | 告警生命周期 |

## 4. 状态与枚举

- `data_batch.status`: `UPLOADING/VALIDATING/AVAILABLE/WARN_AVAILABLE/UNAVAILABLE/ARCHIVED`。
- `strategy_version.status`: `DRAFT/PENDING_REVIEW/PUBLISHED/ARCHIVED/REVIEW_REQUIRED`。
- `backtest_run.status`: `PENDING/QUEUED/RUNNING/COMPLETED/FAILED/UNAVAILABLE/CANCELLED`。
- `order_plan.status`: `PLANNED/PENDING_CONFIRMATION/CONFIRMED/SKIPPED/FILLED/PARTIAL/UNFILLED/CANCELLED/RISK_REJECTED`。
- `risk_state`: `NORMAL/DRAWDOWN_WARNING/STOP_NEW/REVIEW_REQUIRED/RESTORED`。

状态只能由服务层按状态机迁移，并将迁移写入 `audit_event`。

## 4.1 v1.1.0 配置默认值

| 配置项 | 默认值 | 数据落点 |
|---|---|---|
| 历史数据起始日期 | `2016-01-01` | `data_batch` / 运行配置快照 |
| 信号与收益行情 | 后复权；原始 OHLCV 同步保存 | `daily_bar` |
| 主/次基准 | `000300.SH` / `000001.SH` | `backtest_run` |
| 股票池等权基准 | 启用 | `backtest_run.universe_benchmark_enabled` |
| 成交价格模式 | `NEXT_OPEN_ADJUSTED`；买入为次日开盘价 × `1.002`，卖出为次日开盘价 × `0.998` | `cost_config_version` / `backtest_run` |
| 回测部分成交 | `FULL_OR_NONE` | `cost_config_version.partial_fill_mode` |
| 模拟费用 | 双向佣金 `0.30‰`、最低 `5` 元/笔；卖出印花税 `0.50‰`；双向过户费 `0.01‰`；规费/经手费若已包含在佣金中不得重复计提 | `cost_config_version` |

若实际券商交割单与默认费率不同，新增成本配置版本并重新运行；不得修改历史成本版本或历史运行快照。

## 5. 关键约束与索引

1. `order_plan.planned_quantity % 100 = 0` 适用于主板买入计划；卖出零股处理应由 `rule_config_version` 决定并记录。
2. 成交前后 `cash >= 0`、`quantity >= 0`；禁止保证金、负现金、负持仓。
3. 运行必须同时引用 `data_batch_id/strategy_version_id/cost_config_id/rule_config_id`，缺任一不可启动。
4. 运行结果、日报、计划和成交的所有查询默认带 `run_id` 或版本条件，防止混用。
5. 首期不启用表分区，先使用复合索引和版本快照；当 `daily_bar` 或 `audit_event` 任一表超过 500 万行时，按交易日/年度提交带回滚脚本的分区迁移，不改变历史运行口径。
6. 对 `symbol+trade_date`、`run_id+trade_date`、`execution_date+status` 建立复合索引，避免日终查询全表扫描。

## 6. 数据字典与口径

| 字段 | 口径 |
|---|---|
| `amount` | 成交金额，单位元；应与数量和价格校验 |
| `score` | 策略归一化综合分，不代表收益概率或推荐等级 |
| `drawdown` | `1 - equity/high_watermark`，非负比例 |
| `available_quantity` | 遵守 T+1 后可卖数量 |
| `result_usable` | 数据和模型完整、成本配置完整且无未解释限制时为真；不是收益保证 |
| `content_hash` | 对规范化 JSON/文件内容计算的 SHA-256 摘要 |

## 7. 迁移和数据保留

数据库迁移使用版本号、向前迁移脚本和回滚说明；生产迁移前备份并执行影子校验。运行快照和审计保留 3 年，运行日志保留 90 天，原始行情按数据授权期限保留；不得因清理日志破坏运行可复现性。
