# A股短线量化验证系统—运维与应急预案

| 项目 | 内容 |
|---|---|
| 文档名称 | 10-运维与应急预案 |
| 版本 | v1.1.0 |
| 状态 | 基线已决策（待实施） |
| 最后更新时间 | 2026-09-09 |
| 关联文档 | [架构设计](./04-系统架构设计.md)、[发布部署与回滚方案](./09-发布部署与回滚方案.md)、[管理员手册](./12-管理员手册.md)、[测试计划](./08-测试计划与测试用例.md) |

> 本预案用于研究/模拟盘和人工确认实盘前评估。任何故障处置不得自动委托、自动补单或自动扩大仓位。

## 1. 日常运维

### 每个交易日

1. 检查交易日历、前一日备份，以及启用 provider 的连接/授权状态。
2. 收盘后查看数据批次覆盖、质量错误、复权因子和状态数据。
3. 确认日终运行完成，核对运行号、策略/成本/规则版本和日报可用性。
4. 检查现金、持仓、权益、费用和高水位回撤对账；6%/8% 状态需人工确认。
5. 人工查看候选和计划风险，不将系统候选直接视为荐股或自动委托。
6. 次日人工执行后录入实际成交、未成交原因和费用，查看计划/实际偏差。

### 每周/月

- 检查日志容量、备份可恢复性、失败任务趋势和数据源变更。
- 复核规则/费率来源和券商账单，必要时新建成本/规则版本。
- 复盘样本外表现、基准差异、滑点、未成交和行业暴露；异常时将策略置为待复核。
- 检查账户角色、provider Token、导出权限和审计查询，撤销不再使用的账号；Token 只能存在于部署密钥环境。

## 2. 监控指标与告警

| 类别 | 指标 | 固定告警条件 |
|---|---|---|
| 数据 | 批次覆盖、缺失、重复、非正价格、状态缺失 | 错误级 > 0 或覆盖不足 |
| 供应商 | 429/5xx/超时、字段契约、raw manifest 哈希、pilot/full scope | 依赖失败、字段变化、哈希缺失或未经批准开启 full |
| 任务 | 日终耗时、失败率、重试次数、队列积压 | 超过目标/重试上限 |
| 回测 | 运行失败、不可用结果、结果哈希变化 | 固定输入结果变化 |
| 账本 | 现金/持仓不平、负现金/负持仓、对账差异 | 任意非零差异 |
| 订单 | 未成交率、部分成交、计划过期、拒单数 | 单日未成交率 >30% 或连续 5 日平均 >20% |
| 风控 | 6%/8% 回撤、行业/单票超限 | 达到阈值立即通知审核人 |
| 系统 | CPU/内存/磁盘、数据库连接、存储失败 | 连续超阈值 |
| 安全 | 登录失败、越权、敏感日志、异常导出 | 立即告警并审计 |

通知只在有意义的变化、完成、失败或需人工操作时发送；首期渠道为应用内告警和结构化日志，P0 事件由管理员人工通知审核人。

## 3. 日志和审计

日志采用结构化 JSON，包含 `timestamp/level/request_id/run_id/job_id/user_id/event_code`。数据异常需包含批次、证券、日期、字段；不输出 Token、密钥、完整账户号。日志按环境隔离，运行日志保留 90 天，审计和运行快照保留 3 年，原始数据按授权期限保留。审计事件追加写，普通用户和管理员均不能物理删除。

## 4. 备份与恢复

| 对象 | 固定频率 | 备份方式 | 恢复验收 |
|---|---|---|---|
| 关系库 | WAL 每 15 分钟；每日 23:30 增量；每周日 02:00 全量 | 加密备份，异地/离线一份 | 表结构、约束、账本对账 |
| 运行快照 | 每次运行完成 | 内容哈希 + 版本目录 | 相同输入可重现结果 |
| 原始数据 | 每批次导入后；每周日 03:00 校验 | 只读分层存储 | 可按数据版本读取 |
| 配置/迁移 | 每次发布 | 版本库 + 备份 | 可回滚到上一版本 |
| 审计日志 | 追加实时；每日 23:45 归档 | 不可变归档 | 按对象/时间可查询 |

备份不得与生产使用同一密钥；恢复演练记录耗时、缺陷和数据差异。交易计划、成交和审计数据 RPO ≤ 1 小时、RTO ≤ 4 小时；行情数据允许按最近完整批次恢复。

## 5. 应急处置流程

```text
发现 → 分级 → 保护现场/停止受影响任务 → 记录运行号和审计
     → 诊断 → 恢复或回滚 → 对账 → 验证 → 复盘与改进
```

### 5.1 任务失败/超时

暂停下游日报和新计划；查看 `job_run/run_stage` 错误。可重试的 provider 依赖故障使用原幂等键重试；数据、字段契约、权限或规则错误先修复并产生新数据/配置版本。重试后核对没有重复批次、计划、扣款或成交；仍失败则日报标记不可用并通知审核人。

### 5.2 数据异常

停止使用该批次；保留原始文件/响应、脱敏请求摘要、manifest 哈希和错误报告；与上一可用批次比较，不用前值或当前状态静默填补。Tushare 必填数据不完整时保持 `UNAVAILABLE`；AKShare 缺失或差异只作为校验 warning，不修复主批次，也不自动切源。确认数据提供方修复后导入新批次并重新校验；受影响回测和日报重新运行，旧运行保留为不可用/受影响。

### 5.3 策略异常

若发现未来函数、异常高收益、分数漂移、回测与模拟盘偏差连续扩大或净收益低于基准：将策略版本置为待复核，停止新开仓计划，允许既有卖出规则继续；保存输入、代码版本和运行日志，完成根因分析和回归后才能新建发布版本。

### 5.4 账户/交易异常

计划与实际成交不一致、费用不明或无法卖出时，人工以券商实际回报为准；不得自动补单。录入部分成交/未成交和原因，冻结受影响计划，人工核对现金和持仓；必要时由审核人决定是否跳过下一次新开仓。

### 5.5 系统/安全故障

发现数据库损坏、越权、敏感信息泄露或误触自动下单路径时，立即隔离服务和网络出口、停止任务、保留日志、撤销相关凭证（若存在），按 P0 升级。恢复前完成权限和数据完整性审计；不得删除证据。

## 6. 应急联系人和记录模板

首期责任安排：单用户管理员兼值班人和审核人；数据源问题由数据文件提供方负责；应用和数据库由开发/运维角色负责。P0 通过应用内告警、结构化日志和人工通知升级，不自动执行任何交易动作。

事件记录至少包含：事件编号、发现时间、发现人、级别、环境、运行号/批次、影响日期、现象、已采取措施、是否停止新计划、数据/资金对账结果、恢复时间、根因、修复版本、复盘结论。
