# A 股强趋势延续选股策略详细说明书

| 项目 | 内容 |
|---|---|
| 文档名称 | 15-选股策略详细说明书 |
| 策略名称 | 强趋势延续策略（Strong Trend Continuation） |
| 策略基线 | v1.1.0 |
| 文档版本 | v1.0.0 |
| 文档状态 | 研究与实现核对基线 |
| 最后更新时间 | 2026-09-19 |
| 适用市场 | 沪深主板普通 A 股，日线频率 |
| 关联文档 | [需求规格说明书](./02-需求规格说明书.md)、[系统架构设计](./04-系统架构设计.md)、[测试计划](./08-测试计划与测试用例.md)、[专业术语说明](./16-选股策略专业术语说明.md) |

> 本策略仅用于研究、回测、日终验证和人工确认，不构成投资建议，不承诺收益，也不具备自动下单、撤单、补单、转账或自动恢复风险状态的能力。候选股票不等于推荐股票，计划订单不等于券商委托，模拟成交不等于真实成交。

## 1. 文档目的

本文把项目现有需求、领域代码和测试约束整理为一套可解释、可复现、可审计的选股策略。它同时回答以下问题：

1. 每个交易日使用什么数据、按什么时间边界计算；
2. 哪些股票先进入股票池，哪些股票会被排除；
3. 市场环境、个股趋势、短期涨幅和成交额如何共同决定候选；
4. 候选如何评分、排序并处理并列；
5. 仓位、行业、现金、回撤和交易规则如何限制计划；
6. 何时卖出、何时不允许买入、何时必须人工复核；
7. 回测如何避免未来函数、幸存者偏差和不现实成交；
8. 当前代码已经实现什么、部分实现什么、尚未闭环什么。

本文中的“策略目标规则”描述产品基线；“当前实现”描述截至 2026-09-19 的代码事实。二者不一致时，不得把目标规则误写成已经上线的能力。

## 2. 策略摘要

### 2.1 一句话定义

在市场整体处于中短期多头状态时，从历史时点可交易的沪深主板普通 A 股中，选择价格站上短期均线、近期涨幅适中且成交额温和放大的股票，按短中期收益和量能强度进行横截面排名，生成下一交易日的人工确认买入计划，并用短持有期、排名衰退、入场回撤和市场转弱规则管理退出。

### 2.2 策略卡片

| 维度 | 基线定义 |
|---|---|
| 风格 | 短线、趋势延续、横截面排名 |
| 数据频率 | 日线 |
| 信号时点 | T 日收盘数据在信息截止时间前可得后 |
| 最早执行 | T+1，即 T 日后的下一交易日 |
| 市场开关 | 沪深 300 T 日收盘价 > 20 日简单移动平均线 |
| 个股趋势 | T 日收盘价 > 10 日简单移动平均线 |
| 动量区间 | 5 日收益率在 2% 至 12% 之间，包含边界 |
| 量能区间 | 5 日平均成交额 / 20 日平均成交额在 1.2 至 3.0 之间，包含边界 |
| 评分 | 5 日收益排名 50% + 20 日收益排名 25% + 量能比排名 25% |
| 组合上限 | 最多 4 只；同行业最多 2 只 |
| 目标资金 | 初始权益 20,000 元；单票目标 3,500 元；总投资不超过 70% |
| 集中度 | 单票不超过权益 20%；单行业不超过权益 35% |
| 单笔风险 | 不超过总权益 1%，需要有效止损距离 |
| 整数股 | 买入数量为 100 股的整数倍 |
| 卖出触发 | 持有满 3 个交易日、排名跌出候选池后 50%、较入场价下跌 4%、市场开关连续两日关闭，任一满足即触发 |
| 成交模型 | `NEXT_OPEN_ADJUSTED`：买入 T+1 开盘价 × 1.002，卖出 T+1 开盘价 × 0.998 |
| 回测成交 | `FULL_OR_NONE`，不能完整成交则整笔不成交 |
| 回撤控制 | 高水位回撤达 6% 停止新增；达 8% 进入人工复核 |
| 交易边界 | 只支持无杠杆、无融资、无做空的现金多头研究 |

## 3. 策略设计思想

### 3.1 市场过滤

个股短线动量在整体市场偏强时通常更有延续条件。策略先判断沪深 300 是否站上 20 日均线；市场开关关闭时不产生新买入，但已有持仓仍需继续执行卖出判断和风险管理。

### 3.2 趋势确认

个股收盘价高于 10 日均线，代表价格仍处于短期均线之上。该条件只做资格过滤，不直接决定排名。

### 3.3 适度动量

5 日收益下限 2% 用于排除缺乏短期强度的股票；上限 12% 用于避免追逐短期涨幅过大的标的。上下限均包含边界。

### 3.4 温和放量

5 日平均成交额相对 20 日平均成交额的比值用于刻画近期资金活跃度。低于 1.2 表示量能确认不足；高于 3.0 可能属于异常放量或事件驱动，基线策略同样排除。

### 3.5 横截面选择

通过资格过滤的股票不直接按原始数值相加，而是在当日候选集合内部转换为 0 至 1 的排名分数。这使不同量纲的收益率与量能比可以组合，也减少极端值对总分的直接支配。

### 3.6 短持有与快速退出

策略基线将最大持有期设为 3 个交易日，并同时关注排名衰退、入场回撤和市场环境。它不是长期基本面策略，也不假设止损价一定能够成交。

## 4. 时间语义与因果边界

### 4.1 关键时间定义

| 字段 | 含义 | 约束 |
|---|---|---|
| `trade_date` | 行情所属交易日 | 特征只能读取 `trade_date <= T` 的记录 |
| `as_of_date` | 本次信号的观察日，即 T 日 | 股票池、状态、行业和特征都必须按该日重建 |
| `available_at` | 单条数据实际可用于研究的时间 | 必须存在且带时区；晚于截止时间的记录不可见 |
| `information_cutoff_at` | 本次运行允许看到信息的最晚时间 | `available_at <= information_cutoff_at` 才可参与计算 |
| `signal_date` | 信号生成日 | 等于 T 日 |
| `execution_date` | 计划或模拟成交日 | 必须晚于 `signal_date`，通常为 T+1 |

### 4.2 不可违反的规则

1. T 日信号只能使用 T 日及以前的数据。
2. 数据虽然属于 T 日，但若在 `information_cutoff_at` 之后才发布，也不能参与本次信号。
3. T+1 的开盘价、最高价、最低价、收盘价和成交量不能反向影响 T 日候选。
4. 行业、ST、停牌、退市和上市状态必须取 T 日当时有效的历史记录，不能用今天的状态覆盖历史。
5. T 日收盘价只能用于形成信号和参考区间，不能伪装成成交价。
6. 数据、策略、成本、交易规则和运行配置必须固定版本及内容哈希，历史运行不得被新版本覆盖。

### 4.3 未来函数检查

最小回归测试是在 T+1 注入极端价格或成交量，然后重新计算 T 日信号。若候选、分数、排名或计划发生变化，说明存在未来函数，运行必须判为不可用。

## 5. 数据输入与质量门禁

### 5.1 必需数据

| 数据类别 | 关键字段 | 用途 |
|---|---|---|
| 证券主数据 | 证券代码、证券类型、交易所、板块、上市日、退市日 | 主板普通 A 股判断、上市天数、历史退市处理 |
| 日行情 | 日期、原始及复权 OHLC、成交量、成交额、复权因子、可得时间 | 特征、成交模拟、估值和质量校验 |
| 证券状态历史 | 生效日期、ST、停牌、退市整理期 | 按历史时点构建股票池 |
| 行业历史 | 行业代码、生效起止日 | 同行业数量和行业暴露限制 |
| 交易日历 | 是否开市、上一/下一交易日 | 交易日窗口、T+1 和持有期计算 |
| 指数行情 | 沪深 300；可选上证综指 | 市场开关和基准比较 |
| 成本版本 | 佣金、最低佣金、印花税、过户费、滑点 | 计划估算和净收益 |
| 规则版本 | 手数、涨跌停、成交方式、生效日期 | 数量取整和可成交性判断 |

### 5.2 复权口径

产品基线规定：收益和信号使用后复权价格，原始价格保留用于审计。一个运行内不得混用不一致的复权口径；复权因子缺失、非正或使用未来因子回填时，数据批次必须不可用。

### 5.3 阻断性质量错误

以下问题必须 fail closed，不得用 0、前值、当前值或静默删除代替：

- 数据集为空；
- 证券代码或交易日缺失；
- 同一证券、同一日期重复；
- OHLC、成交量、成交额或复权因子必填值缺失；
- 价格或复权因子非正，成交量或成交额为负；
- `available_at` 缺失、无时区或晚于信息截止时间；
- 缺少判断股票池所需的历史状态；
- 基准指数当日快照或足够历史窗口缺失；
- 出现证券在上市日前已有行情等幸存者偏差迹象；
- 成本或交易规则版本缺失。

## 6. 历史股票池

### 6.1 目标规则

在 T 日，证券需依次满足：

1. 证券类型为普通股 `COMMON`；
2. 板块为沪深主板 `MAIN`；
3. T 日不是 ST 或 *ST；
4. T 日没有停牌；
5. T 日不在退市整理期，也未在当日或此前退市；
6. 截至 T 日已上市满 60 个交易日；
7. 有至少 20 个可用交易日的成交额历史；
8. 最近 20 个交易日成交额中位数不低于 20,000,000 元；
9. 所有判断只使用 T 日及以前、且在信息截止时间前可得的数据。

### 6.2 流动性定义

设最近 20 个有效交易日成交额为 `A[t-19], ..., A[t]`：

```text
median_amount_20 = median(A[t-19], ..., A[t])
```

只有 `median_amount_20 >= 20,000,000` 才通过。使用中位数而不是均值，可降低单日异常放量对流动性判断的影响。

### 6.3 排除原因

| 原因码 | 说明 |
|---|---|
| `non_common_stock` | 不是普通股 |
| `non_mainboard_common_stock` | 不是沪深主板普通股 |
| `st` | T 日为 ST 或 *ST |
| `suspended` | T 日停牌 |
| `delist_period` | T 日处于退市整理期 |
| `delisted` | T 日已经退市 |
| `missing_list_date` | 缺少上市日期 |
| `listed_less_than_60_trading_days` | 上市不足 60 个交易日 |
| `insufficient_20_day_amount_history` | 成交额历史不足 20 个交易日 |
| `median_amount_below_20000000` | 20 日成交额中位数低于 2,000 万元 |
| `data_not_available_at_as_of` | 记录在观察日当时不可得 |

### 6.4 当前实现提示

`HistoricalUniverseSelector` 已实现上述主要规则。但当前持久化日终编排并未调用该选择器，而是只按当日存在、非基准、非停牌、非 ST、非退市和上市日不晚于 T 日形成简化股票池。因此“主板、普通股、上市满 60 日、20 日成交额中位数”在日终候选链路中仍未完整闭环。回测订单生成也直接遍历数据中的非基准证券。研究结果必须把这一差异视为已知限制。

## 7. 特征计算

所有窗口均按实际交易日序列计算，不按自然日补齐。若历史长度不足，特征返回缺失，证券不得进入候选。

### 7.1 10 日简单移动平均线

设个股 T 日及此前 10 个交易日收盘价为 `C[t-9] ... C[t]`：

```text
MA10[t] = (C[t-9] + ... + C[t]) / 10
```

资格条件为严格大于：

```text
C[t] > MA10[t]
```

等于 MA10 不通过。

### 7.2 市场 20 日简单移动平均线

设沪深 300 最近 20 个交易日收盘价为 `M[t-19] ... M[t]`：

```text
MarketMA20[t] = (M[t-19] + ... + M[t]) / 20
```

市场开关条件为：

```text
M[t] > MarketMA20[t]
```

等于 20 日均线时市场开关关闭。

### 7.3 5 日收益率

代码使用 T 日收盘价相对 5 个交易日前收盘价：

```text
Return5[t] = C[t] / C[t-5] - 1
```

因此计算需要至少 6 个收盘价观察值。候选范围：

```text
2% <= Return5[t] <= 12%
```

### 7.4 20 日收益率

```text
Return20[t] = C[t] / C[t-20] - 1
```

计算需要至少 21 个收盘价观察值。该特征不做候选阈值过滤，但参与综合评分；缺失时不得用 0 代替。

### 7.5 成交额放大比

设 `Amount` 为成交额：

```text
AvgAmount5[t]  = mean(Amount[t-4]  ... Amount[t])
AvgAmount20[t] = mean(Amount[t-19] ... Amount[t])
AmountRatio[t] = AvgAmount5[t] / AvgAmount20[t]
```

当 20 日平均成交额为 0 或历史不足时，量能比缺失。候选范围：

```text
1.2 <= AmountRatio[t] <= 3.0
```

### 7.6 可靠可买状态

当前特征对象将“非停牌且非涨停”视为 `reliably_buyable`。候选过滤还会独立排除 ST、停牌、退市和涨停。因此任何一个状态为真都不能产生新买入信号。

## 8. 市场开关与候选过滤

### 8.1 市场开关

```text
market_switch_on = MarketClose[t] > MarketMA20[t]
```

- 开关开启：允许继续计算个股买入候选；
- 开关关闭：禁止新增买入；已有持仓仍进行卖出和风险检查；
- 市场数据缺失：不是“关闭”，而是数据不可用，应阻断运行或明确标记不可用。

### 8.2 候选条件

股票必须同时满足以下全部条件：

```text
数据在截止时间前可得
AND 市场收盘价 > 市场 MA20
AND 个股收盘价 > 个股 MA10
AND 2% <= 5 日收益率 <= 12%
AND 1.2 <= 量能比 <= 3.0
AND 非 ST
AND 非停牌
AND 非退市
AND 非涨停
AND 可可靠买入
AND 所有必需特征均非缺失
```

任何一项失败都只表示“不进入候选”，不能把缺失特征填 0 后继续评分。

## 9. 评分与排序

### 9.1 排名分数

对当日全部合格候选，分别对 5 日收益、20 日收益和量能比做降序排名。设候选数为 `N`，某值的降序名次为 `p`：

```text
RankScore = (N - p) / (N - 1), N > 1
```

- 第一名得 1；
- 最后一名得 0；
- 只有一个候选时得 1；
- 数值并列时取并列名次的平均位置，再计算排名分数。

### 9.2 综合评分

```text
Score = 0.50 × Rank(Return5)
      + 0.25 × Rank(Return20)
      + 0.25 × Rank(AmountRatio)
```

综合分范围为 0 至 1。评分只反映候选集合内的相对强弱，不代表上涨概率或预期收益率。

### 9.3 确定性排序

排序键为：

1. 综合分降序；
2. 综合分相同时，证券代码字典序升序。

该规则保证相同输入得到稳定结果。行业约束在排序之后依次应用，不修改原始分数。

### 9.4 组合初选

按排序逐只扫描：

1. 已选数量达到 4 只则停止；
2. 某行业已选 2 只则跳过该行业的后续候选；
3. 通过后加入初选组合并赋予连续名次。

当前实现将缺失行业统一视为同一个 `None` 行业组，因此缺行业的股票最多选 2 只。正式使用前应优先补齐行业历史，而不是把缺失行业当作可忽略状态。

## 10. 卖出规则

### 10.1 触发条件

已有持仓满足任一条件时，T 日生成卖出信号，最早在 T+1 执行：

| 条件 | 公式或定义 | 原因码 |
|---|---|---|
| 最大持有期 | `held_trading_days >= 3` | `MAX_HOLDING_DAYS` |
| 排名衰退 | `rank > candidate_count / 2` | `RANK_OUT_OF_POOL` |
| 入场回撤 | `close <= entry_price × (1 - 4%)` | `ENTRY_DRAWDOWN` |
| 市场转弱 | 市场开关连续 2 个交易日关闭 | `MARKET_SWITCH_OFF` |

### 10.2 多重触发优先级

当前领域代码只返回一个原因；当同日满足多项条件时，优先级为：

```text
MAX_HOLDING_DAYS
→ RANK_OUT_OF_POOL
→ ENTRY_DRAWDOWN
→ MARKET_SWITCH_OFF
```

报告若需要保留全部原因，应在后续版本扩展为原因列表，不能改变既有历史运行。

### 10.3 止损不等于保证成交

入场回撤达到 4% 只会产生卖出计划。如果 T+1 停牌、跌停、缺少价格或保守成交价越界，卖出仍可能失败。此时必须保留仓位、未成交原因和下一次重评估记录，不能伪造成交。

### 10.4 当前回测限制

当前持久化回测订单生成器对每只证券在整个回测区间最多生成一次买入，并以固定 100 股下单；卖出主要按“买入执行日后 3 个交易日形成退出信号、再下一交易日执行”的固定路径生成。排名衰退、4% 入场回撤和市场连续两日关闭尚未在该订单生成器中动态重算。因此当前回测结果不能宣称完整验证了全部卖出规则。

## 11. 仓位与风险控制

### 11.1 基线参数

| 参数 | 默认值 |
|---|---:|
| 初始权益 | 20,000 元 |
| 最大投资比例 | 70% |
| 最大持仓数 | 4 |
| 单票目标市值 | 3,500 元 |
| 单票最大比例 | 权益的 20% |
| 单行业最大比例 | 权益的 35% |
| 单笔风险预算 | 权益的 1% |
| 最小交易单位 | 100 股 |

### 11.2 可用名义金额

对候选股票，理论可分配名义金额为以下约束的最小值：

```text
remaining_total    = max(0, equity × 70% - current_invested)
remaining_position = max(0, equity × 20% - current_symbol_value)
remaining_industry = max(0, equity × 35% - current_industry_value)

notional = min(
  3500,
  remaining_total,
  remaining_position,
  remaining_industry,
  available_cash
)
```

若提供每股止损距离 `stop_distance`：

```text
risk_budget   = equity × 1%
risk_quantity = risk_budget / stop_distance
notional      = min(notional, risk_quantity × price)
```

### 11.3 数量取整与费用

```text
raw_quantity = floor(notional / price)
quantity = floor(raw_quantity / 100) × 100
```

然后逐手检查“买入金额 + 费用”是否超过现金；若超过，则每次减少 100 股，直到现金足够或数量变为 0。

### 11.4 禁止的资金行为

以下任一情况都必须拒绝：

- 账户类型不是现金账户；
- 使用融资或借款资金；
- 杠杆倍数不等于 1；
- 允许做空；
- 买入后现金为负；
- 卖出数量超过可卖数量；
- 计划数量不是 100 股的整数倍。

### 11.5 高水位回撤状态机

```text
drawdown = (high_water_mark - current_equity) / high_water_mark
```

| 状态 | 触发 | 行为 |
|---|---|---|
| `NORMAL` | 回撤 < 6% | 可按其他规则生成新买入 |
| `STOP_NEW` | 回撤 >= 6% 且 < 8% | 停止新增仓位，仍允许风险卖出 |
| `REVIEW_REQUIRED` | 回撤 >= 8% | 停止新增并进入人工复核；状态具有黏性 |

从 `REVIEW_REQUIRED` 恢复必须同时满足：

1. 操作者有授权；
2. 复核清单已完成；
3. 当前权益恢复到“高水位减 6%”以上；
4. 使用两个不同、未重复的确认记录完成二次确认；
5. 全过程写入审计事件。

不得自动恢复，也不得因为回撤回落就静默恢复为正常。

### 11.6 当前日终计划限制

当前日终服务以固定 100 股生成买入计划，只检查累计预估资金是否超过“初始权益 × 最大投资比例”。它尚未在该链路中完整调用通用分配器，因此单票 20%、行业 35%、单笔风险 1%、真实持仓、现金、回撤状态和费用后逐手缩减尚未完全落实。日报当前也固定报告 `risk_state=NORMAL`。这些能力虽有独立领域代码和测试，但不能视为日终端到端已闭环。

## 12. T+1 计划与成交模拟

### 12.1 计划形成

T 日收盘后，系统只生成研究计划：

- `signal_date = T`；
- `execution_date = T+1`；
- 默认方向为 `BUY`；
- 计划有效期目标为 T+1 09:25；
- 计划需要人工确认；
- 计划不能直接调用券商。

当前日终实现尚未知道 T+1 的实际开盘价，因此计划的 `reference_low`、`reference_high` 和费用估算暂时使用 T 日收盘价，且上下界相同。这只是计划阶段的参考值，不是 `NEXT_OPEN_ADJUSTED` 模拟成交价。待 T+1 行情可得后，回测或复盘才按下一节的开盘价模型判断成交。

### 12.2 保守成交价

```text
BUY_PRICE  = T+1_OPEN × 1.002
SELL_PRICE = T+1_OPEN × 0.998
```

调整后的价格还必须落在 T+1 的 `[low, high]` 区间内，否则标为 `OUT_OF_RANGE`，不得成交。

### 12.3 不成交场景

| 场景 | 结果/原因 |
|---|---|
| 缺开盘、最低价、最高价，或停牌 | `MISSING_OPEN_OR_SUSPENDED` |
| 涨停买入或跌停卖出 | `PRICE_LIMIT` |
| 调整后价格超出当日高低价 | `OUT_OF_RANGE` |
| 可成交数量不足且为全成或全不成 | `FULL_OR_NONE` |
| 找不到执行日行情 | `MISSING_EXECUTION_BAR` |
| 订单缺少信号日 | `MISSING_SIGNAL_DATE` |
| 信号日不早于执行日 | `SIGNAL_DATE_NOT_BEFORE_EXECUTION` |

### 12.4 部分成交边界

回测固定采用 `FULL_OR_NONE`。人工成交录入可以记录部分成交，但它是独立事实事件，不能修改原始计划，也不能被当作回测模拟成交。

## 13. 交易成本

### 13.1 默认费率

| 成本项 | 默认值 | 方向 |
|---|---:|---|
| 佣金 | 成交额的 0.30‰，最低 5 元/笔 | 买卖双向 |
| 印花税 | 成交额的 0.50‰ | 仅卖出 |
| 过户费 | 成交额的 0.01‰ | 买卖双向 |
| 滑点 | 买入 +0.20%，卖出 -0.20% | 已体现在成交价模型 |

### 13.2 计算公式

```text
notional       = price × quantity
commission     = max(notional × 0.00030, 5)
stamp_duty     = notional × 0.00050   # 仅 SELL
transfer_fee   = notional × 0.00001
total_cost     = commission + stamp_duty + transfer_fee
```

所有金额计算使用 `Decimal`，不能使用二进制浮点数替代记账金额。费率变化必须创建新成本版本，不能修改历史运行。

## 14. 每日日终流程

### 14.1 标准流程

```text
导入授权数据
→ 原始数据脱敏归档与哈希
→ 标准化与质量门禁
→ 按 T 日重建历史股票池
→ 计算沪深 300 市场开关
→ 计算个股特征
→ 应用候选过滤
→ 横截面评分与行业/数量初选
→ 应用账户、集中度、现金与回撤风控
→ 生成 T+1 计划
→ 人工复核与确认
→ 人工录入实际成交
→ 更新账本与偏差报告
```

### 14.2 日报应至少展示

- 运行号、数据版本、策略版本、成本版本、规则版本；
- T 日、信息截止时间、下一交易日；
- 数据质量结论和阻断问题；
- 市场开关值、沪深 300 收盘价与 MA20；
- 股票池入选和排除原因；
- 每只候选的 MA10、5/20 日收益、量能比、条件真假；
- 三个排名分、综合分、最终排名、行业；
- 当前权益、现金、持仓、行业暴露和回撤状态；
- 每个计划的方向、数量、参考区间、预估费用和拒绝原因；
- “不构成投资建议、不承诺收益、不自动下单”的固定提示。

### 14.3 无候选不是失败

数据质量可用但没有股票通过过滤时，运行状态仍可成功，候选状态应为 `NO_CANDIDATES`。这与数据不可用、系统失败或风控拒绝必须明确区分。

## 15. 回测设计

### 15.1 时间切分

回测必须满足：

```text
start <= train_end < valid_end < oos_start <= end
```

- 训练段：形成或初步选择参数；
- 验证段：比较有限候选配置；
- 样本外段：只做最终检验，不反复调参；
- 时间序列不得随机打乱。

### 15.2 回测真实性要求

1. 每日使用当日历史股票池，而不是今天仍上市的股票列表；
2. 信号日与执行日严格分离；
3. 订单必须有 `signal_date < execution_date`；
4. 停牌、涨跌停、缺价、价格越界和现金不足不得伪造成交；
5. 成交后才更新账本，未成交订单不得扣款；
6. 买入股份当日不可卖，下一交易日结算后才成为可卖数量；
7. 成本逐笔落账，绩效以扣成本净收益为准；
8. 相同输入版本和哈希应产生相同交易序列、净值和指标。

### 15.3 绩效指标

| 指标 | 基线定义 |
|---|---|
| 总收益率 | `末期权益 / 初始权益 - 1` |
| 最大回撤 | 净值相对此前高水位的最大下降比例 |
| 夏普比率 | 日收益均值 / 日收益总体标准差 × `sqrt(252)`；当前实现未扣无风险利率 |
| 胜率 | 已闭合交易中盈利笔数 / 已闭合交易笔数 |
| 盈亏比 | 平均盈利金额 / 平均亏损绝对金额 |
| 平均持有期 | 闭合交易从买入执行日到卖出执行日的平均自然日差；当前代码不是交易日数 |
| 换手率 | 全部成交名义金额 / 初始权益 |
| 总成本 | 全部有效成交的佣金、印花税和过户费之和 |
| 行业暴露 | 买入名义金额按行业占全部买入名义金额的比例；当前实现不是期末市值暴露 |
| 月/年度收益 | 各月/年最后一个权益点相对上一期间末权益的变化 |

权益曲线少于 3 个点或存在非正权益时，指标应标记 `INSUFFICIENT_DATA`，不得包装成有效绩效结论。

### 15.4 评估重点

除总收益外，至少比较：

- 扣成本后收益与沪深 300、上证综指、股票池等权基准；
- 训练、验证和样本外三段的稳定性；
- 最大回撤、连续亏损和恢复时间；
- 成本占毛收益比例；
- 未成交率及其原因分布；
- 行业集中度和单票贡献；
- 参数敏感性，而不是只看单个最优参数点；
- 计划与人工实际成交的价格、数量和费用偏差。

## 16. 参数版本与建议配置结构

策略版本发布后不可编辑，只能复制并产生新版本。建议使用以下结构表达基线参数：

```json
{
  "strategy_code": "strong_trend_continuation",
  "benchmark_symbol": "000300.SH",
  "market_ma_window": 20,
  "stock_ma_window": 10,
  "min_return_5d": "0.02",
  "max_return_5d": "0.12",
  "min_amount_ratio": "1.2",
  "max_amount_ratio": "3.0",
  "score_weight_return_5d": "0.50",
  "score_weight_return_20d": "0.25",
  "score_weight_amount_ratio": "0.25",
  "max_positions": 4,
  "max_per_industry": 2,
  "max_holding_days": 3,
  "drawdown_exit": "0.04"
}
```

当前代码直接可配置的字段是 `max_positions`、`max_per_industry`、`min_return_5d`、`max_return_5d`、`min_amount_ratio`、`max_amount_ratio`、`max_holding_days` 和 `drawdown_exit`。均线窗口、评分权重和基准部分仍是代码或运行配置基线，不应在界面上伪装成已支持的自由参数。

## 17. 计算示例

假设 T 日沪深 300 收盘为 4,100，20 日均线为 4,000，市场开关开启。有三只股票通过股票池与候选过滤：

| 股票 | 5 日收益 | 20 日收益 | 量能比 |
|---|---:|---:|---:|
| A | 8% | 15% | 1.8 |
| B | 10% | 12% | 2.4 |
| C | 5% | 18% | 1.5 |

三只股票每个因子的降序排名分依次取 1、0.5、0：

| 股票 | 5 日排名分 | 20 日排名分 | 量能排名分 | 综合分 |
|---|---:|---:|---:|---:|
| A | 0.5 | 0.5 | 0.5 | 0.500 |
| B | 1.0 | 0.0 | 1.0 | 0.750 |
| C | 0.0 | 1.0 | 0.0 | 0.250 |

因此排序为 B、A、C。若 B 与 A 属于同一行业且该行业已有两只更高分持仓，则它们仍保留原分数，但会在行业约束阶段被跳过。

假设 B 的保守参考价为 10 元，权益为 20,000 元且无持仓：

```text
目标金额             = 3,500
单票 20% 上限        = 4,000
行业 35% 上限        = 7,000
总投资 70% 剩余额度  = 14,000
现金                 = 20,000
可分配金额           = min(...) = 3,500
原始数量             = floor(3,500 / 10) = 350
100 股取整后          = 300 股
```

还需加上买入费用检查现金；若止损距离为每股 1 元，单笔风险预算为 200 元，则风险数量为 200 股，最终数量应进一步缩减至 200 股。

## 18. 决策解释模板

### 18.1 入选解释

```text
600000.SH 在 2026-09-18 进入候选：
- 数据在信息截止时间前可得；
- 沪深 300 收盘高于 MA20，市场开关开启；
- 个股收盘高于 MA10；
- 5 日收益 6.4%，位于 [2%, 12%]；
- 量能比 1.56，位于 [1.2, 3.0]；
- 非 ST、非停牌、非退市、非涨停；
- 综合分 0.73，当日排名第 2；
- 通过持仓数与行业初选；
- 最终数量仍需账户风控和人工确认。
```

### 18.2 排除解释

排除原因应精确到阶段和数值，例如：

```text
000001.SZ 未进入候选：5 日收益 13.2%，超过上限 12%。
600001.SH 未进入股票池：上市 42 个交易日，低于 60 日门槛。
600002.SH 信号合格但计划被拒绝：行业市值将超过权益 35%。
600003.SH 已生成计划但未模拟成交：T+1 涨停，原因 PRICE_LIMIT。
```

不得只显示笼统的“条件不满足”。

## 19. 发布与验证门槛

### 19.1 策略发布前

- 参数范围和权重校验通过；
- 策略版本有变更原因和审核记录；
- 数据、状态、行业、成本和规则版本均可追溯；
- 未来函数、幸存者偏差、T+1、停牌、涨跌停、缺价和现金约束测试通过；
- 相同输入重复运行结果一致；
- 无任何自动委托或券商连接路径。

### 19.2 模拟盘评估前

- 至少 60 个交易日；
- 有效计划尝试不少于 30 笔；
- 无 P0 缺陷；
- 数据质量、成本和计划/实际偏差可解释；
- PostgreSQL、Redis/RQ、备份恢复与审计链路按目标环境完成验证；
- 研究人员和审核人员明确理解当前实现差距。

### 19.3 不应作为通过依据的现象

- 单段历史收益很高；
- 只在训练区间表现良好；
- 忽略交易成本后盈利；
- 用当前成分股回测历史；
- 把未成交订单当成成交；
- 修改参数后覆盖原运行；
- 用少量合成夹具代替真实授权历史数据验证。

## 20. 当前实现完成度

| 能力 | 状态 | 说明 |
|---|---|---|
| 因果特征与信息截止时间 | 已实现 | 特征只读取 T 日及以前且在截止时间前可得的数据 |
| MA10、市场 MA20、5/20 日收益、量能比 | 已实现 | 历史不足时返回缺失并拒绝候选 |
| 候选阈值与状态过滤 | 已实现 | 包含边界；排除 ST、停牌、退市、涨停 |
| 横截面排名、并列规则、代码稳定排序 | 已实现 | 综合权重固定为 50%/25%/25% |
| 最大 4 只、同行业 2 只 | 已实现于策略层 | 是否与真实持仓合并计算仍需端到端闭环 |
| 完整历史股票池 | 部分实现 | 独立选择器完整；日终和回测编排未完整接入 |
| 四类卖出规则 | 部分实现 | 领域函数完整；持久化回测主要使用固定持有期 |
| 通用仓位分配 | 已实现于领域层 | 日终计划未完整接入，当前固定 100 股 |
| 高水位回撤状态机 | 已实现于领域层 | 日终报告当前固定 `NORMAL`，尚未完整接入账户状态 |
| T+1 保守成交、不可成交原因 | 已实现 | 回测强制 `FULL_OR_NONE` |
| T+1 计划生成 | 已实现 | 日终生成研究计划，不连接券商 |
| 计划参考价 | 部分实现 | 当前用 T 日收盘形成单点参考和费用估算，尚未形成独立的保守价格区间 |
| 交易成本 | 已实现 | 佣金、卖出印花税、过户费与滑点 |
| 账本与 T+1 可卖 | 已实现 | 买入后下一交易日才可卖 |
| 绩效指标 | 已实现基础版 | 夏普无无风险利率，持有期按自然日差 |
| 训练/验证/OOS 独立指标 | 尚未完整 | 当前持久化指标主要写入 `FULL` 段 |
| 次基准和股票池等权比较 | 数据模型已有基线 | 当前核心指标结果未完整呈现比较结果 |
| 自动交易 | 明确禁止 | 项目不包含券商委托能力 |

## 21. 推荐补齐顺序

为让策略定义与运行事实一致，建议按以下顺序补齐，不应同时扩大策略范围：

1. 将 `HistoricalUniverseSelector` 接入日终和回测，先解决股票池真实性；
2. 将通用仓位分配器与真实账本、行业历史接入日终计划；
3. 将回撤状态机接入日报和计划确认；
4. 在回测中逐日动态重算全部卖出条件，并允许平仓后重新入选；
5. 让回测使用同一套股票池、策略、风控和订单计划服务，减少日终/回测漂移；
6. 分段输出训练、验证、样本外和多基准指标；
7. 增加参数敏感性、滚动窗口和计划/实际偏差报告；
8. 完成 PostgreSQL、Redis/RQ、真实授权数据和恢复演练后，再评估模拟盘门槛。

## 22. 风险声明

本策略依赖历史价格、成交额和状态数据，无法消除市场结构变化、流动性骤降、跳空、连续涨跌停、停牌、费用变化、数据修订和人工执行偏差。排名分是相对指标，不是预测概率；止损是触发条件，不是成交保证；历史回测不代表未来收益。任何人工操作都必须在独立券商系统中由用户自行判断和执行，本系统不得代替适当性评估、投资决策或交易授权。
