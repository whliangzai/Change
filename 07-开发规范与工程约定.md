# A股短线量化验证系统—开发规范与工程约定

| 项目 | 内容 |
|---|---|
| 文档名称 | 07-开发规范与工程约定 |
| 版本 | v1.1.0 |
| 状态 | 基线已决策（待实施） |
| 最后更新时间 | 2026-09-03 |
| 关联文档 | [需求规格说明书](./02-需求规格说明书.md)、[架构设计](./04-系统架构设计.md)、[数据库设计](./05-数据库设计.md)、[API](./06-API接口设计.md)、[测试计划](./08-测试计划与测试用例.md)、[部署方案](./09-发布部署与回滚方案.md) |

> v1.1.0 已确定首期技术基线：Python 3.12、FastAPI、Pydantic、SQLAlchemy、Alembic、PostgreSQL 16、Redis 7 + RQ、Docker Compose、Parquet/版本化文件目录。所有规范服务于可复现回测、人工确认和无自动下单边界。

## 1. 建议目录结构

```text
quant-validation/
├─ app/
│  ├─ api/                  # API 路由、鉴权、DTO
│  ├─ domain/               # 数据、股票池、信号、风控、成交、账本领域服务
│  ├─ application/          # 回测/日终/导出用例编排
│  ├─ infrastructure/       # 数据库、文件存储、队列、数据源适配器
│  └─ reports/              # 只读报告生成器
├─ migrations/              # 版本化数据库迁移
├─ configs/                 # 非秘密的默认配置和规则样例
├─ tests/                   # unit/integration/e2e/data/regression/security
├─ docs/                    # 技术设计、ADR、运行手册
├─ scripts/                 # 可审计的导入、校验、备份脚本
└─ pyproject.toml / README.md
```

信号计算不得直接访问未来日期；组合风控不得修改信号对象；报告生成器不得写账本。跨层访问通过接口和依赖注入完成。

## 2. 代码与类型规范

- 使用 Python 3.12；代码质量工具固定为 Ruff（格式化/静态检查）、mypy（类型检查）和 pytest（测试）。页面采用 FastAPI + Jinja2/HTMX，首期不引入 TypeScript 前端工程。
- 金额、费率、价格使用 Decimal/`numeric`；数量使用整数；日期使用交易日类型或明确的 `date`，禁止用本地时间推断交易日。
- 函数命名表达业务意图，如 `build_historical_universe(as_of_date)`、`simulate_execution(order_plan, market_snapshot)`；避免 `process()` 等无语义名称。
- 公开函数必须写输入、输出、异常和是否允许副作用；核心纯计算优先无副作用。
- 序列化配置固定字段顺序、单位和小数精度，保存规范化 JSON 后再计算哈希。
- 注释解释业务原因、交易规则和数据口径，不重复代码。

## 3. 领域不变量

1. 回测运行必须绑定数据、策略、成本、规则和基准版本。
2. 信号的 `as_of_date` 不得大于所有特征数据日期；成交日期不得早于 T+1。
3. 买入计划数量按规则配置取整，成交后现金和持仓不可为负。
4. 风控 6%/8% 阈值由账户高水位计算；8% 状态下不能创建新买入计划。
5. 订单状态迁移必须由服务端状态机完成；原始计划和成交记录不可覆盖。
6. 费用逐笔记录，报告净收益以实际费用和滑点扣除后口径为准。

## 4. 分支与提交

- 分支建议：`main`（发布）、`develop`（集成）、`feature/<需求编号>-<短名>`、`fix/<问题编号>-<短名>`、`release/<版本>`。
- 提交格式建议 Conventional Commits：`feat(bt): add next-day execution simulation`、`fix(risk): block new plans at drawdown stop line`、`docs: update trace matrix`。
- 一次提交只解决一个可审查主题；提交正文写动机、风险、测试命令和是否影响历史运行结果。
- 合并前必须通过格式化、类型检查、单元/集成测试、数据回归和安全检查；未执行项写明原因。
- 禁止提交原始账户信息、券商凭证、数据源密钥、个人敏感信息和大体积行情文件。

## 5. 配置与版本管理

配置分为：

| 类别 | 例子 | 管理方式 |
|---|---|---|
| 策略配置 | 均线窗口、收益区间、排名权重 | `strategy_version`，发布后不可改 |
| 交易规则 | 交易单位、涨跌停、交易日历 | `rule_config_version`，来源和生效日 |
| 成本配置 | 佣金、最低佣金、印花税、过户费、滑点 | `cost_config_version`，用户确认 |
| 环境配置 | 数据库地址、队列、日志级别 | 环境变量/密钥库，不进入运行结果以外的代码库 |
| 功能开关 | 模拟盘、导出、任务暂停 | 管理员权限、变更审计 |

启动时校验必填配置、版本和单位；缺失费用或规则时阻断回测，不能使用未声明默认值。

## 6. 日志与异常

- 结构化日志字段：`timestamp/level/service/request_id/run_id/job_id/user_id/event_code/message`。
- 业务异常分为 `ValidationError`、`DataUnavailableError`、`RuleViolationError`、`StateConflictError`、`DependencyError`；API 映射到统一错误码。
- 异常信息必须包含可修复上下文（批次、证券、日期、字段、运行号），不包含 Token、密钥或完整账户信息。
- 重试只用于可重试依赖故障；数据错误、规则违反和幂等冲突不盲目重试。
- 未捕获异常要有告警和关联 `request_id`；错误结果写入 `run_stage/job_run`，不生成伪完成报告。

## 7. 测试和数据回归

- 每个 FR 至少关联一个 `TC-*`，每个核心交易规则有边界和失败用例。
- 固定小型行情集和黄金文件保存版本哈希，用于验证交易序列、净值和指标稳定。
- 属性测试检查现金/持仓不为负、数量取整、信号日期不泄露、账本可对账。
- 不用未来数据或当前存续列表作测试夹具；夹具必须包含退市、ST、停牌和涨跌停样本。
- 测试状态只能写“待执行、执行中、通过、失败、阻塞”，未运行的用例不得标为通过。

## 8. 安全与人工确认边界

- 不实现券商自动委托客户端、API Key 保存、自动撤单和资金划转。
- 人工确认必须是显式动作，包含用户、时间、计划版本、风险摘要和备注；确认不等于成交。
- 管理员操作必须二次确认，尤其是改规则、费率、恢复 8% 熔断和任务重跑；单用户场景采用同一账号的二次确认和检查清单，不自动恢复。
- 导出内容最小化，下载链接短期有效；审计日志追加写并限制读取角色。
- 未来若改为自动生成/下达指令，需另立 PRD，并复核证监会程序化交易管理要求：[官方公告](https://www.csrc.gov.cn/csrc/c101954/c7480579/content.shtml)。

## 9. 代码评审清单

- 是否有明确 FR/API/TC 编号和文档引用？
- 是否可能读取未来日期、当前股票池或幸存者列表？
- 是否将 T 日收盘当作成交价？是否模拟停牌、涨跌停、部分成交和费用？
- 是否使用 Decimal、满足 100 股/现金/单票/行业/回撤约束？
- 是否会覆盖历史运行、计划、成交或审计？
- 是否泄露敏感配置或新增自动下单能力？
- 是否有失败路径、重试边界、监控和回滚说明？
