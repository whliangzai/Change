# 目标开发对话提示词—环境配置补充

在主开发提示词基础上，追加并严格遵守以下规则：

1. 项目根目录为 `F:\PROJECT\Money`。
2. Redis 配置必须从项目根目录的本地 `.env` 读取，不得把密码写入代码、提示词、Docker Compose 文件或 Git。
3. Redis 连接配置为：`REDIS_HOST`、`REDIS_PORT`、`REDIS_PASSWORD`；密码只允许通过环境变量或 `.env` 注入，日志中不得输出密码。
4. 开发启动前，用 PowerShell 或程序执行一次 Redis `AUTH` 和 `PING` 连通性检查；只报告成功/失败，不打印密码。
5. `.env.example` 只能保留空密码占位符；`.env` 已加入 `.gitignore`。
6. Python 固定使用 `D:\Dev\py\python.exe` 创建项目虚拟环境，不要回退到 Python 3.7。
7. 当前 PostgreSQL 服务位于 `D:\postgresql\bin`，本地开发连接信息从环境变量读取；不得把数据库密码提交到代码库。
8. 先读取 `F:\PROJECT\Money` 下的 14 份项目文档，再开始实现；继续遵守人工确认、无自动下单、版本化、审计和测试待执行等原有约束。
