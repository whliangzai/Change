# A股短线量化验证系统—产品流程与页面说明

| 项目 | 内容 |
|---|---|
| 文档名称 | 03-产品流程与页面说明 |
| 版本 | v1.1.0 |
| 状态 | 基线已决策（待实施） |
| 最后更新时间 | 2026-09-03 |
| 关联文档 | [需求规格说明书](./02-需求规格说明书.md)、[架构设计](./04-系统架构设计.md)、[API](./06-API接口设计.md)、[用户手册](./11-用户手册.md)、[管理员手册](./12-管理员手册.md) |

> 本文为文字线框，不是最终 UI 设计稿。页面中的“计划”不等于“委托”，“候选”不等于“推荐”，“已录入”不等于券商成交回报。

## 1. 用户主流程

### 1.1 首次配置

```text
登录 → 完善账户/风险参数 → 配置数据源 → 导入数据 → 查看质量报告
     → 创建策略草稿 → 校验参数 → 审核发布策略版本
```

### 1.2 回测流程

```text
新建回测 → 选择数据/策略/成本/基准 → 设置时间切分
        → 预检 → 排队/运行 → 查看运行状态 → 查看报告/交易明细 → 导出
```

### 1.3 日终计划流程

```text
收盘数据更新 → 质量校验 → 生成信号 → 风控 → 生成日报/计划
             → 人工查看风险 → 确认/跳过计划 → 次日人工执行 → 录入实际成交
             → 查看计划/实际偏差
```

## 2. 页面清单

| 页面编号 | 页面名称 | 主要角色 | 关键需求 |
|---|---|---|---|
| P-01 | 首页/运行总览 | 全部 | FR-OPS-001、FR-RISK-004 |
| P-02 | 数据源与数据质量 | 管理员/研究者 | FR-DATA-001~005 |
| P-03 | 股票池查看 | 全部 | FR-DATA-003~004 |
| P-04 | 策略版本 | 研究者/审核人/管理员 | FR-STRAT-001~006、FR-GOV-001 |
| P-05 | 回测创建 | 研究者 | FR-BT-001~004 |
| P-06 | 回测运行详情 | 全部 | FR-BT-003~006 |
| P-07 | 日报与候选 | 全部 | FR-OPS-001~004、FR-GOV-003 |
| P-08 | 计划订单/人工确认 | 审核人/管理员 | FR-OPS-003、FR-RISK-004 |
| P-09 | 模拟盘/实际成交 | 研究者/审核人 | FR-OPS-005 |
| P-10 | 报告与偏差复盘 | 全部 | FR-BT-005~006、FR-OPS-006 |
| P-11 | 任务与系统监控 | 管理员 | FR-OPS-001、NFR-005 |
| P-12 | 用户/权限/审计 | 管理员/审核人 | FR-GOV-002 |

## 3. 页面文字线框与交互

### P-01 首页/运行总览

```text
[顶部] 系统名 | 数据日期 | 当前用户 | 风险提示
[状态卡] 数据质量：可用/不可用 | 市场开关：开/关 | 回撤：x% | 策略状态：可用/待复核
[账户卡] 总权益 | 现金 | 股票市值 | 今日盈亏 | 高水位
[待办] 待确认计划 | 未成交持仓 | 任务失败 | 数据告警
[快捷入口] 导入数据 / 新建回测 / 查看今日日报 / 录入成交
```

交互：点击“不可用”进入质量报告；点击 6%/8% 回撤状态进入风控详情；禁止把状态卡按钮设计为自动下单按钮。

### P-02 数据源与数据质量

输入区：数据源名称、数据类型、CSV/Parquet 文件位置、授权备注、复权口径、日期范围。v1.1.0 不提供未授权供应商的内置抓取器。

结果区：批次号、数据版本、记录数、覆盖日期、缺失/重复/非正价格/状态缺失、错误/警告级别、可用结论。

错误提示：

- “数据批次不可用：缺少 2026-08-31 交易日，请补齐后重试。”
- “价格校验失败：600000，2026-08-29，close=0；相关回测已阻断。”
- “状态数据缺失：无法判断停牌/ST，禁止将该日期纳入可用股票池。”

### P-03 股票池查看

筛选区：日期、市场、板块、ST/停牌状态、上市天数、流动性门槛。

表格列：证券代码、名称、交易所、入池状态、排除原因、上市交易日数、20 日成交额中位数、历史状态来源。

交互：点击证券查看该日期及前后日期状态；切换“当前股票池/历史股票池”时必须显式显示日期，避免将当前成分用于历史回测。

### P-04 策略版本

```text
[左侧] 版本列表：草稿/待审核/已发布/已归档/待复核
[右侧] 基本信息：版本号、名称、创建人、创建时间、变更原因
[参数组] 市场开关、均线窗口、收益区间、量能比、排名权重、持仓/行业上限、卖出规则
[底部] 参数校验 | 保存草稿 | 提交审核 | 查看差异
```

保存前校验：权重和为 100%；窗口为正整数；收益下限不大于上限；风险阈值 6% 小于 8%；单行业上限不超过 100%。发布后不可编辑，只能复制为新版本。

### P-05 回测创建

必填：数据版本、策略版本、起止日、训练/验证/样本外区间、基准指数、初始权益、成本版本、滑点方式。

预检摘要：数据覆盖范围、缺失比例、股票池范围、是否保留退市证券、成本是否完整、是否存在未来数据风险。

按钮状态：预检失败时“开始回测”禁用；运行中的同一幂等请求不重复创建；提交后显示运行号。

### P-06 回测运行详情

```text
[运行状态] 待运行/排队/运行中/完成/失败/不可用
[配置摘要] 数据版本 | 策略版本 | 成本版本 | 基准 | 时间切分
[进度] 数据校验 → 股票池 → 信号 → 风控 → 成交模拟 → 账本 → 报告
[结果] 净值/回撤图、指标表、订单状态分布、成本明细、数据质量
[操作] 查看交易 / 导出配置 / 导出报告
```

错误提示：“运行完成但结果不可用：复权因子缺失 12 条；可查看错误明细，不能用于绩效结论。”

### P-07 日报与候选

顶部显示报告日期、运行号、数据完整性、市场开关和“不构成投资建议”提示。

候选表列：排名、证券代码、分数、满足条件、5/20 日收益、量能比、行业、风险标签、建议数量、建议参考价、计划状态。

持仓表列：证券、数量、成本价、当前价、持有交易日、浮盈亏、退出条件、卖出可行性、风险提示。

交互：用户可展开“为什么入选/为什么排除”；不可直接从候选行触发委托，只能加入计划后等待人工确认。

### P-08 计划订单/人工确认

状态：计划 → 待确认 → 已确认/已跳过 → 已录入成交/部分成交/未成交/取消。

确认面板必须显示：方向、数量、保守价格区间、预估成本、资金占用、单票/行业占比、市场开关、回撤状态、触发原因和风险提示。

确认前校验：数据仍有效、日期为下一交易日、风险状态未升级、数量仍满足 100 股规则。确认动作需要备注，可批量确认但每条计划单独落审计。

错误提示：“计划已过期：交易日已切换，请重新生成 T+1 计划。”、“风控拒绝：回撤已达到停止线 8%，禁止新增仓位。”

### P-09 模拟盘/实际成交

分为“计划订单”和“实际成交录入”两块。实际成交字段：计划号、证券、方向、成交数量、成交价格、成交时间、佣金、税费、其他费用、未成交数量、原因、凭证备注。

交互：保存后生成偏差摘要；不允许编辑已结算账本记录，只能发起更正记录并由管理员审核。

### P-10 报告与偏差复盘

标签页：策略绩效、基准比较、样本外、交易明细、成本、行业暴露、计划/实际偏差、数据质量。

必须同时显示毛收益与扣成本净收益，但评估状态以净收益为唯一口径；“样本外低于基准”“偏差连续扩大”显示“策略待复核”。

### P-11 任务与系统监控

显示任务名称、计划时间、运行号、状态、耗时、重试次数、错误摘要、数据日期。可重试失败任务，但不可对运行中的任务重复启动；管理员可暂停日终任务，需填写原因。

### P-12 用户/权限/审计

管理员可按用户、角色、动作、对象、时间和结果查询审计；敏感值只显示掩码。审核人可查看计划确认记录，不能删除日志。

## 4. 状态模型

| 对象 | 状态 |
|---|---|
| 数据批次 | 上传中、校验中、可用、警告可用、不可用、已归档 |
| 策略版本 | 草稿、待审核、已发布、已归档、待复核 |
| 回测运行 | 待运行、排队、运行中、完成、失败、不可用、已取消 |
| 计划订单 | 计划、待确认、已确认、已跳过、已成交、部分成交、未成交、取消、因风控拒绝 |
| 风控状态 | 正常、回撤警戒、停止新增、人工复核、已恢复 |
| 日报 | 待生成、生成中、已生成、不可用、已更正 |

状态迁移必须由服务端校验；前端按钮只代表请求，不可直接改状态。

## 5.1 v1.1.0 交互基线

- 回测创建页默认加载 2016-01-01 至最新可用日、沪深 300 主基准、上证综指次基准和股票池等权基准。
- 成交模拟页显示 `NEXT_OPEN_ADJUSTED`：买入参考价为 T+1 开盘价上浮 0.20%，卖出参考价为 T+1 开盘价下调 0.20%；系统同时显示“模拟价，不是委托价”。
- 计划页默认有效至下一交易日 09:25；过期计划按钮变为“重新生成”，不允许继续确认。
- 回测中未能可靠判断部分成交时按 `FULL_OR_NONE` 处理；人工成交录入仍可录入部分成交并生成偏差。
- 单用户页面显示 `USER/REVIEWER/ADMIN` 三个职责标签；8% 回撤恢复按钮必须连续完成“查看账本—查看未成交—填写原因—二次确认”。
- 数据源页首期提供 CSV/Parquet 文件导入，不提供未授权供应商的内置抓取器。

## 6. 全局错误与提示规范

- 数据问题：说明批次、证券、日期、字段、修复动作。
- 风控问题：说明触发阈值、当前值、是否允许卖出/是否需要审核。
- 交易问题：说明计划不等于成交，列出未成交原因和重评估日期。
- 权限问题：统一提示“无权执行该操作”，不泄露对象是否存在。
- 所有页面固定显示：研究用途、不荐股、不承诺收益、不自动下单。
