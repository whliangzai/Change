# A股短线量化验证系统—系统架构设计

| 项目 | 内容 |
|---|---|
| 文档名称 | 04-系统架构设计 |
| 版本 | v1.1.0 |
| 状态 | 基线已决策（待实施） |
| 最后更新时间 | 2026-09-03 |
| 关联文档 | [需求规格说明书](./02-需求规格说明书.md)、[数据库设计](./05-数据库设计.md)、[API](./06-API接口设计.md)、[发布部署与回滚方案](./09-发布部署与回滚方案.md)、[运维预案](./10-运维与应急预案.md) |

> v1.1.0 已确定首期技术基线：Python 3.12、FastAPI、Pydantic、SQLAlchemy、Alembic、PostgreSQL 16、Redis 7 + RQ、Docker Compose、Parquet/版本化文件目录。系统定位是研究、验证和人工确认工具，架构不得包含自动下单链路。

## 1. 架构目标与约束

### 1.1 目标

- 输入、参数、交易规则和成本版本化，使回测可复现、可解释、可对账。
- 用交易日期、数据可得时间和信息截止时间共同隔离信号计算与成交模拟，阻断未来函数。
- 用不可变运行快照和审计日志记录每次策略/数据/人工操作。
- 通过模块边界支持本地 MVP 后续扩展，但不提前引入自动交易或复杂机器学习。

### 1.2 约束

1. 范围仅为沪深主板普通 A 股，其他品种在配置层拒绝。
2. T 日收盘生成信号，T+1 产生计划；信号模块不可读取 T+1 数据。
3. 风控模块可以缩减或拒绝仓位，不得改变信号分数。
4. 成交模拟只判断可执行性和成本，不产生交易信号。
5. 报告模块只读取运行快照，不修改策略或账户账本。
6. 实盘只输出人工确认清单，不持有券商凭证、不发起委托。

## 2. 总体架构

```text
[Web/CLI 用户界面]
          │ HTTPS/本地 API
      [API 层 + 鉴权]
          │
 ┌────────┴─────────┐
 │ 应用编排/任务队列 │────[审计与通知]
 └──────┬───────────┘
        │
 [领域服务层]
 数据与股票池 │ 信号 │ 组合风控 │ 成交模拟 │ 回测账本 │ 日报报告
        │
 [关系库] [行情/文件存储] [运行快照/导出]
        │
 [合法数据源适配器]   （无券商下单适配器）
```

MVP 采用模块化单体：Python 3.12 + FastAPI + PostgreSQL 16 + SQLAlchemy/Alembic + Redis 7/RQ + Parquet 文件存储，使用 Docker Compose 编排应用、Worker、数据库和队列。SQLite 不作为运行环境，仅可用于单元测试夹具。当数据量或并发增长时，再拆分数据处理和回测 Worker。

## 3. 模块划分与边界

| 模块 | 输入 | 输出 | 不负责 |
|---|---|---|---|
| 市场数据模块 | 数据源文件/API、来源配置 | 标准化行情、状态、日历、数据版本 | 产生交易信号 |
| 数据质量模块 | 标准化数据 | 错误/警告、质量门禁、批次状态 | 静默修补关键缺失 |
| 股票池模块 | 日期、历史状态、流动性配置 | 入池集合、排除原因 | 修改原始数据 |
| 信号模块 | T 日及以前股票池和行情 | 特征、资格、分数、触发条件 | 决定买入数量 |
| 组合与风控 | 信号、账户快照、风险配置 | 目标仓位、拒绝/缩减原因、风控状态 | 改分数或模拟成交 |
| 成交模拟 | 计划订单、T+1行情、交易规则、成本 | 成交/未成交、费用、成交原因 | 创建信号 |
| 回测账本 | 成交结果、现金、持仓 | 日快照、权益、净值、回撤 | 直接改变历史成交 |
| 实盘/模拟盘记录 | 人工确认、实际成交 | 差异、账本调整记录 | 自动读取券商账户 |
| 报告模块 | 不可变运行快照 | 日报、指标、导出 | 修改底层运行结果 |
| 治理审计 | 用户动作、状态迁移、配置变更 | 审计事件、版本关系 | 删除关键日志 |

## 4. 核心数据流与时间隔离

### 4.1 收盘到计划

```text
T 日行情落库（记录 available_at）
 → 以 `information_cutoff_at` 检查该数据在 T 日收盘前可得
 → 数据质量门禁
 → 以 T 日的历史状态构建股票池
 → 只读取 <=T 的数据计算特征/分数
 → 风控读取 T 日账户快照和配置
 → 生成 execution_date = next_trade_date(T) 的计划订单
```

成交模拟在 `execution_date` 上读取开盘/可成交价格、停牌、涨跌停和交易日规则；默认采用 `NEXT_OPEN_ADJUSTED`，买入为开盘价上浮 0.20%，卖出为开盘价下调 0.20%；不得把 T 日收盘作为成交价。每一运行保存 `as_of_date`、`execution_date`、`available_at`、`information_cutoff_at` 和数据快照标识。数据表中若 `available_at > information_cutoff_at`，该记录不得参与 T 日信号。

### 4.2 数据不可用传播

`数据批次不可用 → 股票池不可用 → 信号不可用 → 回测/日报不可用`。警告级问题可继续时必须在报告展示影响范围；禁止以缺失值补零导致“看似成功”。

## 5. 任务调度

| 任务 | 固定时点/触发条件 | 幂等键 | 失败处理 |
|---|---|---|---|
| 交易日历更新 | 每周一 18:30；规则变更后立即补充版本 | calendar_version+date | 重试，失败不覆盖旧版本 |
| 日行情导入 | 数据文件目标于 18:00 前落地；落地后立即导入 | source+trade_date+dataset_version | 质量门禁，错误告警 |
| 数据质量校验 | 导入后 | batch_id | 错误阻断下游 |
| 日终信号/日报 | 数据可用后 | strategy_version+as_of_date+data_version | 可重试，不重复计划 |
| 回测运行 | 用户触发 | request_id/配置哈希 | 队列运行，保留失败日志 |
| 备份 | WAL 每 15 分钟；每日 23:30 增量；每周日 02:00 全量 | backup_date+scope | 重试并告警 |

## 6. 运行与一致性设计

- 每个运行写入 `run_id`，绑定数据、策略、成本、规则和基准版本；运行配置及结果采用 append-only 语义。
- 订单状态迁移使用服务端状态机和幂等键；同一计划不能重复扣减现金。
- 账本更新使用事务：成交记录、费用、持仓、现金和日快照要么一起成功，要么一起回滚。
- 运行中断后从阶段检查点恢复或完整重跑，重跑结果必须与原运行一致；不允许手工改净值。

## 7. 扩展性

1. 数据源通过 Adapter 接口隔离供应商字段和授权，标准模型不绑定供应商。
2. 策略通过版本化 `StrategyDefinition` 接口输出特征/信号，风控与成交模拟保持独立。
3. 交易规则、费用和板块作为配置版本，可增加品种但必须经过范围和合规评审。
4. 回测 Worker 可水平扩展；结果写入运行快照后由报告读取，避免报告阻塞计算。
5. 未来若接入券商，只能新增隔离的人工辅助导入或只读对账适配器；自动委托需另立项目。

## 8. 安全、合规与审计

- 首期固定使用本地账户登录；密码用 Argon2id 哈希，访问 Token 有效期 30 分钟、刷新 Token 有效期 7 天，并支持注销、停用和管理员撤销。
- 敏感数据（数据源密钥、导出文件）使用系统密钥库/环境变量，日志仅输出掩码；不保存券商密码。
- RBAC + 最小权限；管理员配置变更、策略发布、计划确认、成交录入和更正都记录审计。
- 审计事件包含操作者、角色、时间、动作、对象、前值摘要、后值摘要、请求号和结果。
- 导出文件可加运行号和校验哈希；访问日志不记录完整个人敏感信息。
- 不建立自动下单网络出口；若未来变更，必须重新评估证监会程序化交易规定和交易所要求。来源：[证监会程序化交易管理规定（试行）](https://www.csrc.gov.cn/csrc/c101954/c7480579/content.shtml)。

## 9. 可观测性

指标：数据批次成功率、质量错误数、日终任务耗时、回测吞吐、队列长度、运行失败率、订单状态分布、账本对账差异、报告生成延迟、6%/8% 风控触发次数。

日志采用结构化 JSON，必须含 `request_id`、`run_id`、`batch_id`、`user_id`（可匿名化）和业务日期；错误日志不得泄露密钥和完整账户敏感信息。

## 10. 外部规则与费用配置

当前规则应引用并定期复核：[上交所交易规则（2026 年修订）](https://www.sse.com.cn/lawandrules/sselawsrules2025/stocks/exchange/c/c_20260424_10816482.shtml)、[深交所交易规则（2026 年修订）](https://docs.static.szse.cn/www/lawrules/rule/trade/current/W020260424690713155663.pdf)。收费口径参考：[上交所收费说明](https://one.sse.com.cn/onething/gptz/)、[深交所收费标准](https://investor.szse.cn/marketServices/deal/payFees/index.html)，但最终以用户券商账单和实施时官方版本为准。规则变化通过 `rule_config_version` 进入运行快照，不直接改历史结果。
