# A股短线量化验证系统—发布部署与回滚方案

| 项目 | 内容 |
|---|---|
| 文档名称 | 09-发布部署与回滚方案 |
| 版本 | v1.1.0 |
| 状态 | 部署基线保留；当前为本地验证阶段，部署联调与恢复演练暂不执行 |
| 最后更新时间 | 2026-09-05 |
| 关联文档 | [架构设计](./04-系统架构设计.md)、[数据库设计](./05-数据库设计.md)、[开发规范](./07-开发规范与工程约定.md)、[测试计划](./08-测试计划与测试用例.md)、[运维预案](./10-运维与应急预案.md) |

> 部署基线已决策：Docker Compose + Python 3.12/FastAPI + PostgreSQL 16 + Redis 7/RQ + Parquet/本地版本化目录。首期只支持人工确认，不降低数据备份、审计和自动下单隔离要求。

> 当前阶段范围（2026-09-05）：本文件保留后续部署与回滚基线，当前项目仅在本地使用。Docker、PostgreSQL 16、Redis 7/RQ、容器健康检查和生产恢复演练不纳入本阶段验收，也不作为本地功能进度的待办项。

## 1. 环境划分

| 环境 | 用途 | 数据 | 外部能力 |
|---|---|---|---|
| DEV | 开发调试 | 脱敏/合成数据 | 禁止生产密钥和券商连接 |
| TEST | 自动化、回归、演练 | 固定黄金数据集和历史样本 | 可模拟数据源、无自动委托 |
| SIM | 模拟盘/日终试运行 | 经授权的行情和人工成交 | 只读数据源、无自动下单 |
| LIVE-PRE（门槛后启用） | 小额实盘前人工评估 | 独立数据/账本 | 仅人工确认清单；未通过门槛时不部署 |

环境之间不得共用数据库、密钥或运行目录。任何从生产导出的数据必须脱敏并记录审计。

## 2. 发布前置条件

- 需求、架构、数据库、API、测试、运维和追踪矩阵版本一致。
- P0 测试用例已执行并有证据；未来函数、账本、越权、自动下单路径无开放缺陷。
- 数据源授权、复权口径、历史退市状态和行业分类来源已确认。
- 实际券商费率、最低佣金、印花税/过户费口径已填写为成本配置版本；未配置不允许可用回测。
- 规则版本复核：[上交所 2026 年交易规则](https://www.sse.com.cn/lawandrules/sselawsrules2025/stocks/exchange/c/c_20260424_10816482.shtml)、[深交所 2026 年交易规则](https://docs.static.szse.cn/www/lawrules/rule/trade/current/W020260424690713155663.pdf)。
- 完成数据库备份、恢复抽检、运行快照导出和回滚演练；未执行项标注待执行。

阶段验收注记（2026-09-04）：Compose 文件在补齐 `AUTH_SECRET_KEY`、`DATABASE_URL`、`POSTGRES_PASSWORD` 等必需变量后通过旧版 `docker-compose config --quiet`；请求的 `docker compose` 子命令不可用，Docker daemon 无法连接，未执行容器部署/健康检查，loopback readiness 在无 Redis 服务时超时。PostgreSQL 16、Redis 7、RQ worker、容器迁移/readiness/import/daily/backtest/idempotency smoke 均保持待执行或受阻。独立 SQLite 迁移链已从 `0001` 升级至 `0003` 并通过 `alembic check`，不代表 PostgreSQL 16 迁移演练通过。Ruff format 已通过（`121 files already formatted`）。

## 3. 配置项

| 配置 | 示例/说明 | 秘密 |
|---|---|---:|
| `APP_ENV` | `DEV/TEST/SIM/PROD` | 否 |
| `DATABASE_URL` | 环境专属连接串 | 是 |
| `OBJECT_STORE_PATH` | 行情/报告存储 | 否 |
| `QUEUE_URL` | 异步任务队列 | 是 |
| `DATA_SOURCE_SECRET_REF` | 密钥库引用 | 是 |
| `DATA_IMPORT_MODE` | `AUTHORIZED_CSV_PARQUET` | 否 |
| `DATA_HISTORY_START` | `2016-01-01` | 否 |
| `BENCHMARK_PRIMARY/SECONDARY` | `000300.SH/000001.SH` | 否 |
| `RULE_CONFIG_VERSION` | 规则配置版本 | 否 |
| `COST_CONFIG_VERSION` | 用户/券商成本版本 | 否 |
| `EXECUTION_PRICE_MODE` | `NEXT_OPEN_ADJUSTED` | 否 |
| `SLIPPAGE_BUY/SELL` | `0.002/0.002` | 否 |
| `PARTIAL_FILL_MODE` | `FULL_OR_NONE` | 否 |
| `DEFAULT_INITIAL_EQUITY` | `20000.00` | 否 |
| `MAX_INVESTMENT_RATIO` | `0.70` | 否 |
| `MAX_POSITIONS` | `4` | 否 |
| `DRAWDOWN_WARNING/STOP` | `0.06/0.08` | 否 |
| `LOG_LEVEL` | `INFO`，故障时临时 `DEBUG` | 否 |
| `EXPORT_RETENTION_DAYS` | `1095` | 否 |

策略参数、规则、成本不得通过环境变量覆盖已发布的版本；环境变量只选择版本或连接资源。

## 4. 部署步骤

1. 冻结发布版本和迁移清单，记录代码提交、镜像摘要和文档版本。
2. 在目标环境执行备份并验证备份文件哈希和可恢复性。
3. 部署应用镜像和 Worker，先不启用日终调度。
4. 执行数据库迁移：备份 → 迁移 → 结构校验 → 索引/约束检查；破坏性迁移固定安排在非交易日 00:00–04:00，首期大表只新增索引和字段，不做在线删除。
5. 导入/校验规则、成本和交易日历配置，不覆盖历史配置。
6. 运行健康检查、身份权限检查、固定黄金数据回归、账本对账和无自动下单检查。
7. 执行一次 TEST/SIM 小范围日终演练，核对运行号、日报、计划和审计。
8. 启用日终任务，观察首个交易日完成情况；首日由审核人人工核对全部计划。
9. 发布记录写入变更日志，失败则按第 7 节回滚。

## 5. 定时任务

| 任务 | 固定时点/触发条件 | 依赖 | 结果 |
|---|---|---|---|
| 更新交易日历 | 每周一 18:30；规则变更后立即 | 官方规则来源 | `trade_calendar` 新版本 |
| 导入收盘行情 | 数据文件落地后，目标 18:00 前；本地处理从数据可用起 ≤5 分钟 | 授权 CSV/Parquet 文件 | `data_batch` |
| 质量校验 | 导入后 | data batch | 可用/不可用 |
| 生成信号/日报 | 质量通过后 | 数据、策略、成本、规则 | `run_id`、日报、计划 |
| 备份 | WAL 每 15 分钟；每日 23:30 增量；每周日 02:00 全量 | DB、文件存储 | 备份清单 |

任务使用业务日期和幂等键；重复调度不得重复生成计划、成交或扣减现金。

## 6. 上线检查清单

- [ ] 数据覆盖到目标交易日，质量错误为 0 或已审批的警告。
- [ ] 交易规则、交易日历和成本版本已锁定且有来源/生效日。
- [ ] 资金、100 股单位、单票/行业/投资比例和回撤阈值校验正常。
- [ ] 回测/日报只使用 T 日及以前特征，买入计划日期为 T+1。
- [ ] 停牌、涨跌停、缺价和未成交提示可见。
- [ ] 计划确认与实际成交权限正确；无券商自动下单路由和凭证。
- [ ] 备份恢复抽检、监控告警、日志脱敏和审计查询正常。
- [ ] 用户收到“不荐股、不承诺收益、不自动下单”提示。

验收证据（2026-09-04）：20 万条日线性能测量 `elapsed_seconds=4.842594`、`peak_bytes=242022348`（约 230.8 MiB，tracemalloc），输入 `bars.csv` SHA-256 为 `f73ea72f8bcf44dd1472aea4caf7a95647110b7854a608bc68dc0352d6ac4566`，`run_id=run_39b710280504`，`content_hash=e66de388329e22d4f753d58349bbb50823e59e1162041f43e2fd37aaf069449a`；该数值仅作实测记录，不宣称达到 5 分钟目标。隔离 SQLite 恢复检查中迁移、manifest 2 文件哈希和账本复核为 `VERIFIED`，但 `run_reproducibility=NOT_CHECKED`、`ready=false`，未执行真实恢复覆盖。

## 7. 回滚方案

### 7.1 应用回滚

发现新版本导致接口错误、日报失败或计算结果变化时：暂停日终任务 → 标记受影响运行不可用 → 保留日志和快照 → 切换到上一镜像 → 运行健康检查和黄金数据回归 → 恢复任务。不得删除新版本运行数据。

### 7.2 数据库回滚

优先使用向前兼容迁移和修复迁移；只有确认无法修复时才在维护窗口恢复备份。恢复前冻结写入、导出新旧审计、核对运行/订单/成交数量；恢复后执行结构和账本对账。任何已录入的实际成交不得因应用回滚而丢失；若冲突，建立更正记录并由管理员审核。

### 7.3 配置回滚

规则、成本、策略均切换到上一已发布版本，历史运行仍绑定原版本；回滚原因、操作者、前后版本、生效日和影响范围写入审计。不得直接修改 JSON 以“修复”历史结果。

## 8. 故障处理分级

| 级别 | 示例 | 动作 |
|---|---|---|
| P0 | 账本不平、未来函数、越权、误触自动委托 | 立即停止任务/新计划，人工复核并升级 |
| P1 | 日报延迟、数据源失败、回测不可用 | 阻断受影响日期，重试或切换已验证数据源 |
| P2 | 单页面错误、导出延迟 | 记录工单，允许其他功能继续 |

发布后的首个交易日观察 1 个完整日终周期；首期告警渠道为应用内告警 + 结构化日志，P0 同步人工通知。交易计划/成交/审计数据 RPO ≤ 1 小时、RTO ≤ 4 小时；运行快照和审计保留 3 年，原始数据受授权期限约束。
