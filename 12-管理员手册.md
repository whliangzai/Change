# A股短线量化验证系统—管理员手册

| 项目 | 内容 |
|---|---|
| 文档名称 | 12-管理员手册 |
| 版本 | v1.1.0 |
| 状态 | 基线已决策（待实施） |
| 最后更新时间 | 2026-09-03 |
| 关联文档 | [需求规格说明书](./02-需求规格说明书.md)、[架构设计](./04-系统架构设计.md)、[数据库设计](./05-数据库设计.md)、[API](./06-API接口设计.md)、[发布部署与回滚方案](./09-发布部署与回滚方案.md)、[运维预案](./10-运维与应急预案.md) |

> 管理员可配置和排障，但不能绕过人工确认、回撤熔断、审计和数据质量门禁；本系统不提供自动下单。

## 0. v1.1.0 固定运行基线

- 技术：Python 3.12、FastAPI、PostgreSQL 16、Redis 7/RQ、Docker Compose。
- 数据：合法授权 CSV/Parquet 文件，历史范围从 2016-01-01 起；保留原始未复权行情，后复权用于收益/信号。
- 基准：沪深 300（`000300.SH`）为主基准，上证综指（`000001.SH`）为次基准，另算股票池等权。
- 成交：`NEXT_OPEN_ADJUSTED`，买入开盘价上浮 0.20%，卖出开盘价下调 0.20%；回测采用 `FULL_OR_NONE`，人工成交允许部分成交。
- 成本：佣金 0.30‰ 双向、最低 5 元/笔；卖出印花税 0.50‰；过户费 0.01‰ 双向；佣金已含规费时不重复计费。
- 权限：单用户账号兼任 `USER/REVIEWER/ADMIN`；8% 回撤恢复需要检查清单和二次确认，不自动恢复。
- 模拟盘：至少 60 个交易日且有效计划尝试不少于 30 笔；不足时自动延长观察。

## 1. 用户和权限

在“用户/权限”创建账号、停用账号和分配 `USER/REVIEWER/ADMIN`。遵循最小权限：普通用户研究和录入；审核人发布策略和确认计划；管理员管理数据源、配置、任务和审计。账号停用要记录原因，不能删除其历史审计。

定期检查登录失败、长期未使用账号和导出权限。密码不得明文保存；数据源密钥只保存密钥引用，日志和页面显示掩码。

## 2. 数据源和数据批次

1. 创建数据源记录，填写文件来源、授权备注、字段映射、复权口径和更新频率；首期仅接收合法授权 CSV/Parquet，密钥（如文件存储需要）放入密钥库/环境变量。
2. 导入日线 OHLCV、复权因子、交易日历、停复牌、ST、上市日期、行业和退市状态。
3. 查看质量报告，确认交易日连续、无重复、价格有效、状态完整、复权因子存在。
4. 将批次标记为可用、警告可用或不可用；错误级问题不得被手工标为可用而没有审计和风险接受。
5. 归档旧批次但保留读取能力，不能覆盖运行曾引用的数据版本。

规则来源和费用来源需记录 URL、发布日期/生效日、复核人和复核日期。当前可参考上交所、深交所及券商实际说明：[上交所交易规则](https://www.sse.com.cn/lawandrules/sselawsrules2025/stocks/exchange/c/c_20260424_10816482.shtml)、[深交所交易规则](https://docs.static.szse.cn/www/lawrules/rule/trade/current/W020260424690713155663.pdf)、[上交所收费说明](https://one.sse.com.cn/onething/gptz/)、[深交所收费标准](https://investor.szse.cn/marketServices/deal/payFees/index.html)。

## 3. 策略和成本治理

- 检查策略参数边界：评分权重和为 100%，5 日收益下限≤上限，窗口为正，回撤警戒 < 停止线。
- 策略草稿提交审核后，审核人比较版本差异、样本外结果和数据范围；发布后不可编辑。
- 费用配置必须包含佣金、最低佣金、卖出印花税、过户费/登记结算相关费和双向滑点；缺失则回测不可用。
- 规则配置包含股票范围、交易日历、主板交易单位、停牌/涨跌停处理、T+1 和价格精度；变化产生新版本。
- 若样本外净收益低于基准或实盘偏差连续扩大，将策略置为“待复核”，不得通过改报告字段恢复为可用。

## 4. 任务管理

查看 `job_run`：任务、业务日期、运行号、开始/结束、状态、重试次数和错误摘要。重试前判断错误是否可重试；同一幂等键重试，不手工复制任务。暂停日终任务需写原因和预计恢复时间，并通知审核人。

任务顺序固定为：行情导入 → 质量校验 → 股票池 → 信号 → 风控 → 计划/日报 → 人工成交录入。质量失败时不得强制执行下游。

## 5. 风控与计划管理

检查账户高水位、回撤、现金、持仓、单票和行业暴露。达到 6% 时确认系统未生成新买入；达到 8% 时确认状态为人工复核。恢复时由审核人确认数据、策略、账户和实际成交对账，填写恢复原因和证据；不能直接改数据库状态。

计划订单只允许状态机迁移。管理员可以取消错误计划或更正成交，但必须创建更正记录，保留原记录和审计。管理员也不能将“计划”改为“已成交”来代替实际成交回报。

## 6. 日志、审计和导出

按用户、角色、动作、对象、运行号、时间和结果检索审计。重点关注：策略/规则/成本发布、回撤恢复、计划确认/跳过、成交录入/更正、任务重试、数据批次手工状态、导出和权限变更。导出文件加运行号和哈希，使用短期授权下载；日志不显示秘密。

## 7. 故障排查

| 现象 | 排查顺序 | 处置 |
|---|---|---|
| 日报未生成 | 数据批次→质量→任务阶段→队列→报告 | 数据错误则阻断并修复新批次；依赖失败才重试 |
| 回测结果不可用 | `result_usable` 原因→数据/成本/规则版本 | 先修复输入，历史结果保留不可用标识 |
| 现金/持仓不平 | 成交→费用→账本→快照→人工更正 | 冻结新计划，导出账本，建立更正并复核 |
| 计划重复 | 幂等键→任务运行→状态迁移审计 | 保留一条有效计划，禁止重复扣款，记录事件 |
| 成交偏差扩大 | 实际成交→滑点/费用→未成交→数据质量 | 将策略置待复核，通知审核人 |
| 权限异常 | Token/角色→对象权限→审计→登录日志 | 停用账号/撤销凭证，按安全事件处理 |
| 规则变化 | 官方来源→生效日期→规则版本 | 新建配置版本，回测/日报绑定新版本，不改历史 |

## 8. 备份、恢复与发布

按 [发布部署与回滚方案](./09-发布部署与回滚方案.md) 执行：发布前全量备份和恢复抽检，部署后健康检查、黄金数据回归、账本对账和无自动下单检查。数据库迁移优先使用向前兼容方式；不得使用破坏性脚本直接删除运行/订单/审计。

## 9. 合规边界

证监会《证券市场程序化交易管理规定（试行）》对通过计算机程序自动生成或下达指令等行为设置监管要求：[官方公告](https://www.csrc.gov.cn/csrc/c101954/c7480579/content.shtml)。本系统仅生成人工审核清单；如有人要求接入券商自动委托、自动撤单或资金划转，管理员应拒绝并要求另立 PRD、重新安全和合规评审。
